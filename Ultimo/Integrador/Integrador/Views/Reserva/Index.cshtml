@model IEnumerable<Integrador.Models.Reserva>

@{
    ViewData["Title"] = "LISTADO DE  MIS RESERVAS";
}

<h1>LISTADO DE MIS RESERVAS</h1>

<p>
    <a asp-action="Create">Registrar nueva reserva</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CostoReserva)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaReserva)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HoraReserva)
            </th>
            @* <th>
                @Html.DisplayNameFor(model => model.IdInstalacionDeportivaNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdUperfilNavigation)
            </th> *@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CostoReserva)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaReserva)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HoraReserva)
            </td>
            @* <td>
                @Html.DisplayFor(modelItem => item.IdInstalacionDeportivaNavigation.IdInstalacionDeportiva)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdUperfilNavigation.IdUperfil)
            </td> *@
            <td>
                    <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.IdReserva">Edit</a> |
                    <a asp-action="Details" class="btn btn-primary" asp-route-id="@item.IdReserva">Details</a> |
                    <a asp-action="Delete" class="btn btn-danger" asp-route-id="@item.IdReserva">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
